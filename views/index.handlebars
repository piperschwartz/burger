<img src="../../assets/img.jpg" width="200" height="300" id="img">

<h1>Eat-Da-Burger!</h1>

{{!-- need to fix the code so that the devoured buttons shows up after a new burger is added, also make it an editable field --}}
<ul>
  {{#each burgers}}
    {{#unless this.devoured}}
        {{this.id}}. {{this.burger_name}}
      <div class="form-group">
    <button type="button" id="devoured" data-id="{{this.id}}"> Devoured!<br>
  </div>
    {{/unless}}
  {{/each}}

   </li>
</ul>


{{!-- create button that says "Devour it!" and be able to press it to add to the devoured list. --}}

<form class="create-form">

  <div class="form-group">
    <label for="newburger"></label>
    <input type="text" id="newburger" name="burger_name">
  </div>

 
{{!-- need to add code so that the submit button adds it to the screen --}}
  <button type="submit">Submit</button>
</form>
<h2>Enter a burger you would love to eat, then Click the "Submit" Button</h2>
